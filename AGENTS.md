# AGENTS.md — Generated by SDD Pipeline

## Context

Next.js 16 App Router, React 19, TypeScript 6 strict, Tailwind CSS 4, next-intl,
Vitest/Testing Library, Playwright/axe-core, and typed environment reporting.

## Tasks

Implement `openspec/changes/align-blog-loading-skeleton/tasks.md` in dependency order.
Use the feature-branch chain `PR 1 → PR 2` within the 800 changed-line budget; tests stay
with the behavior they verify. Preserve blog/content strategy and keep production scope
limited to `app/[locale]/(pages)/blog/loading.tsx`.
