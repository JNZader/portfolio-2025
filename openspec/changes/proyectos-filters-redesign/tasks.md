# Tasks: Proyectos Filters Redesign

> **TDD discipline**: strict RED → GREEN → REFACTOR. Run `npm run test:run` after every task; a RED task is done only when its new tests fail for the expected reason (missing component/behavior), and a GREEN task is done only when the full suite passes.
>
> **Size exception (APPROVED)**: The user approved a single-PR size exception for ~950 changed lines (raw, tests included), superseding the 800-line review budget for this change. This resolves design.md Open Question #1 — ship as ONE PR with work-unit commits, not a 2-PR chain. See "Review Workload Forecast" below.
>
> Technical artifacts in English.

## Phase 1: Foundation

- [x] 1.1 Add `@radix-ui/react-popover` to `package.json` dependencies and install (`npm install @radix-ui/react-popover`). Verify `npm run build` still compiles before any source change.
- [x] 1.2 Baseline: run `npm run test:run` on `main` (`c21ae20`) and confirm a green suite before writing any new test, so every later failure is attributable to this change.

## Phase 2: RED — Failing Tests First

- [x] 2.1 RED `FilterChip`: create `__tests__/integration/components/FilterChip.test.tsx` — renders a native `<button type="button">` with `role="checkbox"`; `aria-checked` mirrors the `selected` prop; bare check icon with `data-testid="filter-check"` and `aria-hidden="true"` present only when selected; class list contains `min-h-11` and `min-w-11` (≥44px bounding box); unselected class list contains `text-foreground/80` and **no** `text-muted-foreground`; selected class list contains `border-primary/50 bg-primary/10 text-primary`; Enter/Space (click) invokes `onToggle`. (Spec: "Shared FilterChip semantics and touch target", "De-grayed unselected chip state".)
- [x] 2.2 RED `SourceSegmentedControl`: create `__tests__/integration/components/SourceSegmentedControl.test.tsx` — one `role="radiogroup"` with localized `aria-label` (from `Projects.sourceLabel`); exactly three `role="radio"` options (All/Curated/GitHub); `aria-checked="true"` only on the value prop; roving tabindex (checked segment `tabIndex=0`, others `-1`); ArrowRight/ArrowDown moves to next with wrap, ArrowLeft/ArrowUp to prev with wrap, Home/End jump to first/last; focus-follows-selection (moving focus calls `onChange`); single-select flip. (Spec: "SourceSegmentedControl promotes the source facet".)
- [x] 2.3 RED `TechFilterBar`: create `__tests__/integration/components/TechFilterBar.test.tsx` — with a fixture of >8 distinct techs: exactly the top 8 by frequency render as chips, ordered frequency-desc with `localeCompare` tie-break, and re-render is stable; a selected tech outside the top 8 is pinned into the visible bar; "More" trigger label shows `+(N − V)` and exposes `aria-haspopup="listbox"` + `aria-expanded="false"`; with ≤8 techs no "More" trigger renders; wrapper is `role="group"` with `aria-label` from `Projects.techBarLabel`. (Spec: "TechFilterBar renders a deterministic top-N plus pinned selections".)
- [x] 2.4 RED `TechDropdown`: create `__tests__/integration/components/TechDropdown.test.tsx` — trigger activation (Enter/Space/click) opens popover, flips `aria-expanded="true"`, and moves focus into the filter input; typeahead case-insensitively filters remaining techs; no match renders localized `techNoResults` and zero `role="option"`; listbox has `aria-multiselectable="true"` and options expose `aria-selected`; ↑/↓ move `aria-activedescendant` while focus stays in the input; Enter/Space toggles the active option (calls `onToggleTech`) without closing; Esc closes and returns focus to the trigger. (Spec: "TechDropdown is searchable, keyboard-navigable, and focus-managed".)
- [x] 2.5 RED layout composition: rewrite `__tests__/integration/components/ProjectsFilterChips.test.tsx` — no "Filtros" toggle button and no collapsible panel in the document; search input, source segmented control, and tech chip bar all visible on initial render (both locales); clear button absent with no active filters, appears when any filter is active, and resets state + URL; live markup exposes `data-region` hooks `filter`, `search`, `control`, `tech-bar`. (Spec: "Always-visible projects filter layout".)
- [x] 2.6 RED skeleton parity: extend the proyectos skeleton block in `__tests__/integration/components/VisualBatch5QuickWins.test.tsx` — `loading.tsx` contains `data-region="tech-bar"` placeholders (chip-shaped + a More-trigger shape), the `control` skeleton is segmented-control-shaped (`h-11 w-full sm:w-64`), and no toggle-button-shaped (`h-12 w-28`) placeholder remains; preserved hooks `filter`/`search`/`control` still asserted; single `role="status"` wrapper and `aria-hidden` decorative descendants unchanged. (Spec: "Loading skeleton mirrors the new filter layout" + MODIFIED "Skeleton semantics".)
- [x] 2.7 RED URL persistence: add integration cases (in the rewritten `ProjectsFilterChips.test.tsx`) — initial render from `useSearchParams` with `?q=…&tech=A,B&source=github` (where `B` is a dropdown-only tech) hydrates search value, checks the GitHub radio, and pins both `A` and `B` as selected chips in the visible bar; `source=all` omits the `source` param; clear empties `q`/`tech`/`source`. (Spec: "URL persistence round-trips all filter dimensions".)
- [x] 2.8 RED i18n parity: create `__tests__/unit/messages/projects-filters.test.tsx` (convention: `__tests__/unit/messages/project-conversion.test.ts`) — new keys `sourceLabel`, `techBarLabel`, `techMore`, `techMoreAria`, `techSearchPlaceholder`, `techNoResults` exist in both `messages/en.json` and `messages/es.json` with non-empty copy; `{count}` interpolates in `techMore`/`techMoreAria` for both locales. (Spec: "Bilingual i18n parity for new filter strings".)
- [x] 2.9 RED blog parity: update `__tests__/integration/components/BlogFilterChips.test.tsx` — blog "All" + category chips expose `role="checkbox"` with correct `aria-checked` and the bare `filter-check` icon when selected; category selection, search, and clear behavior unchanged (existing assertions kept). (Spec: "Blog parity when FilterChip is extracted".)
- [x] 2.10 RED gate: run `npm run test:run` — confirm all tests from 2.1–2.9 fail (missing modules/keys/markup) and **no pre-existing test regressed** beyond the intentionally rewritten files.

## Phase 3: GREEN — Implementation in Dependency Order

- [x] 3.1 GREEN `FilterChip`: create `components/ui/FilterChip.tsx` per design contract (`FilterChipProps`: `selected`, `onToggle`, `children`, `className?` merged via `cn()`); base `min-h-11 min-w-11 px-4 text-sm font-medium`, focus-visible ring; selected `border-primary/50 bg-primary/10 text-primary hover:bg-primary/15`; unselected `border-border bg-card text-foreground/80 hover:text-foreground hover:border-foreground/30`; bare lucide `Check` (`h-3.5 w-3.5`, `aria-hidden`, `data-testid="filter-check"`) only when selected. Plain function component — no `useMemo`/`useCallback` (React Compiler). Task 2.1 passes.
- [x] 3.2 GREEN `SourceSegmentedControl`: create `components/projects/SourceSegmentedControl.tsx`; export `type ProjectSource = 'all' | 'sanity' | 'github'`; `role="radiogroup"` + `aria-label={t('sourceLabel')}`; three `role="radio"` segments with FilterChip-aligned palette, joined geometry (first `rounded-l-md`, last `rounded-r-md`, middles `rounded-none`, `-ml-px` border collapse), `min-h-11 flex-1 sm:flex-initial px-4`; roving tabindex + Arrow/Home/End handling with focus-follows-selection; wrapper `flex w-full sm:w-auto`. Task 2.2 passes.
- [x] 3.3 GREEN `TechFilterBar`: create `components/projects/TechFilterBar.tsx` implementing the design formula (`TOP_N = 8`; freq map over `projects.flatMap(p => p.tech)`; sort freq desc then `localeCompare`; `visible = top8 ∪ pinned`, pinned sorted `localeCompare` and appended; `remaining = all \ visible`); wrapper `<div role="group" aria-label={t('techBarLabel')} data-region="tech-bar" className="flex flex-wrap gap-2">`; render a `FilterChip` per visible tech; render `TechDropdown` iff `remaining.length > 0` (stub the import with the real component from 3.4 — implement 3.3 and 3.4 together if needed to compile, but land tests per task). Task 2.3 passes.
- [x] 3.4 GREEN `TechDropdown`: create `components/projects/TechDropdown.tsx` on `@radix-ui/react-popover` per design (`Popover.Root/Trigger asChild/Portal/Content`, `side="bottom" align="end" sideOffset={4} collisionPadding={8}`, `w-72 max-h-80 overflow-y-auto bg-popover`); trigger chip `{t('techMore', { count })}` + `ChevronDown`, `aria-haspopup="listbox"`, `aria-expanded`, `aria-controls`, `aria-label={t('techMoreAria', { count })}`; `onOpenAutoFocus` prevented → focus filter input (`aria-label`/`placeholder` = `t('techSearchPlaceholder')}`); `role="listbox"` `aria-multiselectable="true"` `aria-activedescendant`; options `role="option"` `aria-selected` `min-h-11` with bare `Check` when selected; ↑/↓ move active descendant, Enter/Space toggles without closing, Esc/outside-click close via Radix; typeahead = case-insensitive `includes`, query cleared on close; no-match → `<p>{t('techNoResults')}</p>`. Task 2.4 passes.
- [x] 3.5 GREEN `ProjectsClient` rewrite: modify `components/projects/ProjectsClient.tsx` — delete `showFilters`, `activeFiltersCount`, `chipClassName`, the Filter `Button` + count `Badge`, the entire `#project-filters` collapsible panel, and now-unused `Filter`/`Check` lucide imports; import `ProjectSource` from `SourceSegmentedControl`; compose row 1 (`SearchInput` `flex-1` + `SourceSegmentedControl` + trailing ghost "Clear" when `hasActiveFilters`, `flex-col gap-4 sm:flex-row`) and row 2 (`TechFilterBar`); add `data-region="filter"` on the filter container and `data-region="control"` on the radiogroup wrapper; keep `searchQuery`/`debouncedSearchQuery`, `selectedTechs`, `updateURL`, debounce effect, `toggleTech`, `handleSourceChange`, `clearFilters` (minus `setShowFilters`), `hasActiveFilters`, filtered-projects memo, results row (`aria-live` count + badges), grid, and empty state **byte-for-byte in behavior**. Tasks 2.5 and 2.7 pass.
- [x] 3.6 GREEN i18n keys: add the six new `Projects.*` keys to `messages/en.json` and `messages/es.json` with the exact copy from design.md. Task 2.8 passes.
- [x] 3.7 GREEN skeleton: modify `app/[locale]/(pages)/proyectos/loading.tsx` — restructure the filter region per design (`data-testid="proyectos-loading-filters"` wrapper, `flex-col gap-4`; row 1 `data-region="search"` + `data-region="control"` skeletons; new `data-region="tech-bar"` row with 8 chip skeletons `h-11 w-20 rounded-md` + More skeleton `h-11 w-24 rounded-md`; all `aria-hidden="true"`); change `control` skeleton from `h-12 w-28` to `h-11 w-full sm:w-64`; keep single `role="status"` wrapper, hero/result-count/card regions untouched. Task 2.6 passes.
- [x] 3.8 GREEN blog parity: modify `components/blog/BlogFilters.tsx` — replace local `chipClassName` with the shared `FilterChip` for "All" + category chips (counts passed via `children`); no changes to toggle panel, URL contract, or copy. Task 2.9 passes.

## Phase 4: Cleanup

- [x] 4.1 Prune dead i18n keys: repo-wide search for `filters`, `sourceHeading`, `techHeading`, `techHint` under the `Projects` namespace (include blog components and all tests); extend `__tests__/unit/messages/projects-filters.test.tsx` with a usage-guard assertion; only then delete verified-unused keys from both catalogs. Keep any key still referenced (`Blog.filters` is a separate namespace and stays). (Spec scenario: "Pruned keys are verified unused first".)
- [x] 4.2 Remove residual toggle references: grep for `showFilters`, `activeFiltersCount`, `project-filters`, `chipClassName` across `app/`, `components/`, `__tests__/`, `e2e/`; delete or rewrite stragglers (including stale snapshots) so no reference to the old toggle/panel survives.
- [x] 4.3 Update `e2e/tests/accessibility-interactions.spec.ts`: replace any "Filtros" toggle-panel interactions with the always-visible flow.
- [x] 4.4 REFACTOR pass: re-read the four new components for duplication (class strings shared between `FilterChip` and segments can be exported from `FilterChip.tsx`); confirm no `useMemo`/`useCallback` in new components; run Biome format/lint and fix.

## Phase 5: E2E and Final Verification

- [x] 5.1 Create `e2e/tests/proyectos-filters.spec.ts` — full flow: search → select source segment → toggle tech from bar → toggle tech from dropdown → clear; assert filtered counts and URL params at each step; refresh round-trip restores state including a dropdown-only tech pinned visible; axe scan of the filter region in light and dark themes (no color-contrast violations); bounding-box check ≥44×44 for every chip, segment, and dropdown option; skeleton `data-region` parity vs. the live page; 320px viewport: all three segments visible, non-overlapping, ≥44px tall. (Spec scenarios: URL round-trip, axe contrast, mobile segments, skeleton parity.)
- [x] 5.2 Run `npm run test:run` — full integration/unit suite green.
- [x] 5.3 Run the Playwright suite (`npx playwright test`) — new + updated e2e green in both locales. (Local run: chromium + mobile-chrome green; firefox/webkit browsers not installed locally — deferred to CI. Pre-existing, unrelated homepage failures in `accessibility-interactions.spec.ts` and the `DATABASE_URL`-blocked `npm run build` are environment limitations, documented in the apply-progress artifact.)
- [x] 5.4 Run `npm run build` and Biome checks — clean.
- [x] 5.5 Walk the spec scenario checklist: every Scenario in `specs/ui/spec.md` maps to at least one passing test from Phases 2/5; record any gap before handoff to sdd-verify.

## Review Workload Forecast

| Metric | Estimate |
|---|---|
| Production code (new components + `ProjectsClient` + skeleton + blog + i18n + dep) | ~490 changed lines |
| Tests (1 rewritten, 4 new integration, 1 new messages test, e2e new + updated) | ~460 changed lines |
| **Total raw** | **~950 changed lines** |
| Review budget | 800 lines |
| **Decision** | **`size:exception` APPROVED by user — single PR, ~950 lines, tests count toward the total** |

- Delivery: **single PR** (no 2-PR chain), composed of work-unit commits in Phase order: (1) dep + FilterChip + its tests, (2) SourceSegmentedControl + tests, (3) TechFilterBar + TechDropdown + tests, (4) ProjectsClient rewrite + composition/URL tests + i18n keys, (5) skeleton + parity tests, (6) blog parity + tests, (7) cleanup + e2e. Tests ride with the behavior they verify (work-unit-commits rule).
- Risk: Medium — raw total exceeds budget but is exception-approved; production code alone is well under budget; each commit is independently reviewable.
- Rollback: single `git revert` of the PR; no data-model/URL-contract changes, so old and new URLs stay interoperable.
